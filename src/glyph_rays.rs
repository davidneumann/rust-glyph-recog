pub struct GlyphRays{
    pub width:i32,
    pub height:i32,
    pub l2r: Vec<i32>,
    pub t2b: Vec<i32>,
    pub r2l: Vec<i32>,
    pub b2t: Vec<i32>,
    pub m2l: Vec<i32>,
    pub m2t: Vec<i32>,
    pub m2r: Vec<i32>,
    pub m2b: Vec<i32>
}
